/* eslint semi: 0 */
/**
 * update ref model file `../generates/<%= filename %>` by follow command:
 * `sails generate dbm <%= filename %> --table <%= table %><%= _extra %>`
 */

module.exports = Object.assign({}, require('../generates/<%= filename %>'), {

});
